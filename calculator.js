const amount=document.getElementById("loanAmount"), interest=document.getElementById("interestAmount"), total=document.getElementById("totalAmount"); function money(n){return new Intl.NumberFormat("en-ZA",{style:"currency",currency:"ZAR"}).format(n)} function update(){if(!amount)return;let a=Math.min(3000,Math.max(1000,Number(amount.value)||2500));amount.value=a;interest.textContent=money(a*.40);total.textContent=money(a*1.40)} if(amount){amount.addEventListener("input",update);update()}